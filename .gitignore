# Personal data/image exports dropped at the repo root — not part of the
# app, and not something that should end up in git history. Doesn't affect
# app/public/favicon.png etc. — already-tracked files aren't untracked by
# a later gitignore rule.
*.csv
/*.jpg
/*.jpeg
/*.png

.DS_Store

# Claude Code's own local session state, not part of the project.
.claude/
