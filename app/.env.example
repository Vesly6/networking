
# Base URL of the deployed server/ instance, no trailing slash. Only needed
# for a real deployment — leave unset for local dev (defaults to
# http://localhost:4000). Not secret: gets baked into the client bundle
# either way, since it's just where the app looks for its backend.
VITE_API_BASE_URL=https://api.yourdomain.com
