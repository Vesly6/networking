// Copied from app/src/constants.ts — just the one constant the demo
// actually needs (cell fill palette + auto-colored dropdown options from
// CSV import).
export const PRESET_COLORS = ['#fecaca', '#fed7aa', '#fef08a', '#bbf7d0', '#bfdbfe', '#ddd6fe', '#fbcfe8', '#e5e7eb'];
